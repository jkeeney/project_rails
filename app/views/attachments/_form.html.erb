<%= form_for [ @article] do |f| %>
<p>
  <% @article.attachments.each do |image| %>
    <p><%= image_tag image.url %></p>
  <% end %>
  <%= f.label :image, "Attach a New Image" %><br />
  <%= f.file_field :image %>
</p>
<% end %>
